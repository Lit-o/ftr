let initialState = {
    isEmpty: true,
    moduleRecoveryTime: "about a month",
}

const inventoryReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default inventoryReducer;